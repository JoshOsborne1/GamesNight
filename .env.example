# Game Night Controller Environment Variables
# Copy this file to .env and fill in your values

# ============================================
# WebSocket Server Configuration (for server)
# ============================================

# The port the WebSocket server listens on (default: 3001)
# Most platforms set PORT automatically, but you can override
WEBSOCKET_PORT=3001

# ============================================
# Client Configuration (for Vite frontend)
# ============================================

# The URL that the frontend uses to connect to the WebSocket server
# This MUST start with wss:// if your frontend is served over HTTPS

# Local development:
VITE_WS_URL=ws://localhost:3001

# Production examples:
# VITE_WS_URL=wss://your-app.railway.app
# VITE_WS_URL=wss://your-app.herokuapp.com
# VITE_WS_URL=wss://ws.your-domain.com

# ============================================
# Platform-Specific Notes
# ============================================

# Railway: Use your railway app URL with wss://
# Render: Use your render app URL with wss://
# Heroku: Use your heroku app URL with wss://
# Fly.io: Use your fly app URL with wss://

# For split deployments (Vercel + separate WS server):
# Point VITE_WS_URL to your WebSocket server, not the Vercel app
